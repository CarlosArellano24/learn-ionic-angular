import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carlosafarellano.LearnIonicAngular',
  appName: 'ionic-social',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
